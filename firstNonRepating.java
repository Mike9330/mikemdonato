fun firstRepeating(list: String) {
	List used;
	for (int i =0; i < list.size(); i++) {
		if (used.contains(list[i])) {
			return list[i];
		} else {
			used.push(i);
		}
	}
}

fun firstNonRepeatingChar(list:String) {
	List used;
	for (int i =0; i < list.size(); i++) {
		if (used.contains(list[i])) {
			
		}
	}
}